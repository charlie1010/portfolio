Purpose: learn the tooling and build my portfolio site

Tools:

VS Code = editor

Terminal = run commands

Node/npm = run web tooling

Git = save checkpoints

Current status: VS Code practice folder created, files created successfully